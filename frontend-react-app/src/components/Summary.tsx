import React, { useState } from 'react';
import './Summary.css';
import { Entry } from './types';

function ListEntries({ entries } : {entries : Entry[]}) {

    return (
        <div className='summary-component-main-container'>
        </div>
    );
}

export default ListEntries;